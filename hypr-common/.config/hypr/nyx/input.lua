-- Preserve layout, model, touchpad, appearance and power settings.
-- Replace the current keyboard options: Caps Lock becomes an additional Escape.
hl.config({ input = { kb_options = "caps:escape" } })
